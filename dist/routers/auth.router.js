import { Router } from "express";
import { changePassword, forgotPassword, getAllUsers, getMe, getSingleUser, login, logout, register, resendOTP, resetPassword, updateUserByAdmin, updateUserProfile, verifyOTP, } from "../controllers/auth.controller.js";
import { authenticate, authorize } from "../middlewares/auth/protection.js";
import { authRateLimiter } from "../middlewares/rate.limiter.js";
import catchAsync from "../utils/catch-async.js";
const authRouter = Router();
authRouter.post("/register", authRateLimiter, catchAsync(register));
authRouter.post("/verify-otp", authRateLimiter, catchAsync(verifyOTP));
authRouter.post("/resend-otp", authRateLimiter, catchAsync(resendOTP));
authRouter.post("/login", authRateLimiter, catchAsync(login));
authRouter.get("/me", authenticate, catchAsync(getMe));
authRouter.get("/", authenticate, authorize("admin", "cso"), catchAsync(getAllUsers));
authRouter.get("/:userId", authenticate, authorize("admin", "cso"), catchAsync(getSingleUser));
authRouter.post("/forgot-password", authRateLimiter, catchAsync(forgotPassword));
authRouter.post("/reset-password/:token", authRateLimiter, catchAsync(resetPassword));
authRouter.post("/logout", catchAsync(logout));
authRouter.patch("/update-profile", authenticate, catchAsync(updateUserProfile));
authRouter.patch("/users/:userId", authenticate, authorize("admin"), catchAsync(updateUserByAdmin));
authRouter.patch("/change-password", authenticate, catchAsync(changePassword));
export default authRouter;
//# sourceMappingURL=auth.router.js.map